---
name: cheatsheet
description: URL을 입력받아 해당 내용을 기반으로 치트시트 문서를 생성한다.
user_invocable: true
arguments:
  - name: url
    description: 치트시트를 만들 대상의 URL
    required: true
---

# 치트시트 생성 스킬

## 작업 절차

1. `$ARGUMENTS.url`에서 웹 페이지 내용을 가져온다.
2. 핵심 내용을 추출하여 치트시트 마크다운 문서를 작성한다.
3. `cheatsheet/` 폴더에 `[주제]-cheatsheet.md` 파일명으로 저장한다.
4. 문서가 완성되면 README.md에 해당 치트시트 링크를 추가한다.

## 문서 작성 규칙

- **존댓말을 사용하지 않는다.** 해라체(~한다/~이다)로 작성한다.
- 불필요한 서론이나 배경 설명 없이 바로 본론으로 들어간다.
- 각 항목은 간결하게, 한눈에 파악할 수 있도록 작성한다.

## 문서 구조

반드시 아래 순서를 따른다:

```markdown
# [주제] 치트시트

> 원본: [URL]($ARGUMENTS.url)

## 기본 개념
- 핵심 개념과 용어를 간결하게 정리한다.
- 표나 목록을 활용하여 한눈에 볼 수 있게 한다.

## 옵션
- 주요 옵션/설정/파라미터를 표로 정리한다.
- | 옵션 | 설명 | 기본값 | 형식으로 작성한다.

## 사용 예시
- 실제 사용 예시를 코드 블록으로 보여준다.
- 기본 사용법부터 고급 사용법 순으로 나열한다.
- 각 예시에는 짧은 설명을 붙인다.
```
